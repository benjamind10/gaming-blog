<ol class="post-list">
  {{#each releases.[0].results as |release|}}
  <li>
    {{log release}}
    {{> game-releases release}}
  </li>
  {{/each}}
  {{#each posts as |post|}}
  <li>
    {{> post-info post }}
  </li>
  {{/each}}
</ol>